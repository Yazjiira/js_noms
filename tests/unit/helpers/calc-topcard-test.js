import {
  calcTopcard
} from '../../../helpers/calc-topcard';
import { module, test } from 'qunit';

module('CalcTopcardHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = calcTopcard(42);
  assert.ok(result);
});
