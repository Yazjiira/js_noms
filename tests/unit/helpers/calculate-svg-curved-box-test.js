import {
  calculateSvgCurvedBox
} from '../../../helpers/calculate-svg-curved-box';
import { module, test } from 'qunit';

module('CalculateSvgCurvedBoxHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = calculateSvgCurvedBox(42);
  assert.ok(result);
});
